---
import { Markdown } from 'astro/components';
import Page from './Page.astro';

const { content } = Astro.props;
const { title, description, publishDate, author } = content;
---

<Page title={title} description={description}>
  <slot />
</Page>
